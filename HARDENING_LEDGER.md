# gt-dataslicer hardening ledger

Living issue ledger for the CODE_REVIEW.md mission. This file records current classifications from the working tree and should be updated as new slices are completed.

Latest verification on 2026-06-18: `node --check src/gt_dataslicer/ui/web/app.js` passed; focused UI asset and launcher tests passed after close-guard notification hardening; in-app Browser rendered probe verified Enter/Space derived-transform reorder; native Pywebview close-guard probe verified a blocked close keeps the window open and shows localized status/run-step guidance; native Pywebview cancel-completion probe verified running -> canceling -> cancelled rendered states; Browser responsive pressure probe verified 480px and, after an extreme-narrow CSS fallback/cache-bust, 240px viewports have no horizontal overflow; final `python -m pytest -q` passed for 345 collected tests; `git diff --check` reported only line-ending warnings; no `gt-dataslicer-pywebview-*` temp folders remained.

Follow-up regression verification on 2026-06-18: comparison with `origin/main` found the Caseware IDEA-style summary feature had been removed. Summary CLI/config/UI/engine/runner behavior was restored and checked with `node --check src/gt_dataslicer/ui/web/app.js`, `python -m pytest tests/unit/test_config.py -q`, `python -m pytest tests/unit/test_engine.py -q`, `python -m pytest tests/unit/test_runner.py -q`, `python -m pytest tests/unit/test_ui_api.py -q`, `python -m pytest tests/unit/test_ui_assets.py -q`, `python -m pytest tests/integration/test_cli.py -q`, `python -m pytest tests/unit/test_freezing.py -q`, and `git diff --check`.

| ID | Status | Severity | Hypothesis / finding | Affected paths | Evidence / docs | Correction | Regression coverage | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | Confirmed, fixed | P1 | XLSX formulas without cached values could be staged as blanks; OpenPyXL does not evaluate formulas. | `src/gt_dataslicer/inputs.py` | OpenPyXL behavior; formula staging tests. | Explicit formula/cache policy and workbook checks. | `tests/unit/test_inputs.py` | Cached results can still be stale because Excel owns formula evaluation. |
| A2 | Confirmed, fixed | P1 | DuckDB reject records can overcount physical rejected rows and can mix scans. | `src/gt_dataslicer/engine/duckdb_engine.py` | DuckDB reject tables; reject integration tests. | Count distinct physical rows scoped to the primary input scan; dedupe reject exports. | `tests/integration/test_cli.py`, `tests/unit/test_engine.py` | DuckDB reject table schema changes would need revalidation. |
| A3 | Confirmed, fixed | P1 | Selecting fewer columns could change CSV validation behavior through projection pushdown. | `src/gt_dataslicer/engine/duckdb_engine.py` | DuckDB scan/projection behavior; malformed unselected column tests. | Materialize typed CSV source once for full-row validation when needed. | `tests/integration/test_cli.py` | Repeated source-scan optimization remains a broader architecture topic. |
| A4 | Regression found, restored | P1 | Numeric Caseware IDEA-style summary aggregation already existed on `main` and was accidentally removed during hardening. | `src/gt_dataslicer/config.py`, `src/gt_dataslicer/cli.py`, `src/gt_dataslicer/engine/duckdb_engine.py`, `src/gt_dataslicer/runner.py`, `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/*` | Comparison with `origin/main` showed `--resumir/--summarize`, `--somente-resumo/--summary-only`, group-by/totals options, UI controls, summary query generation, and queue summary output naming. | Restored shared summary options, grouped/count/total summary query export, summary-only mode, queue summary paths, UI controls, config load/save, and artifact planning so summaries participate in overwrite/input/lookup collision checks. | `tests/integration/test_cli.py`, `tests/unit/test_config.py`, `tests/unit/test_engine.py`, `tests/unit/test_runner.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py` | Summary totals retain the existing DOUBLE-based total behavior; decimal precision policy can be revisited separately if product requirements demand exact decimal totals. |
| A5 | Confirmed, fixed | P1 | XLSX sheet staging filenames could collide after sanitization. | `src/gt_dataslicer/inputs.py` | Sanitized worksheet-name collision tests. | Deterministic unique staged names. | `tests/unit/test_inputs.py` | None known. |
| A6/B5 | Confirmed, fixed | P1 | Outputs, reports, rejects, split XLSX files, inputs, lookups, and destructive ZIP cleanup needed one safety model. | `src/gt_dataslicer/runner.py`, `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/inputs.py` | Artifact collision tests; UI overwrite API tests; ZIP deletion timing reproduction with an extracted XLSX formula-cache failure; native Pywebview overwrite-confirmation retry, ZIP delete-confirmation, and encrypted-ZIP password probes. | Shared planned artifact helper, collision checks, atomic/sibling temporary commits where feasible, UI overwrite confirmation, preflight detection for existing later split XLSX siblings such as `output_002.xlsx`, and ZIP source deletion only after all extracted supported members resolve successfully. | `tests/unit/test_runner.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_artifacts.py`, `tests/unit/test_inputs.py`; Pywebview runtime probe confirmed existing-output prompt text and successful confirmed retry; Pywebview notice probe confirmed ZIP delete warning text and `confirm_delete_zip_after_extract` flag; Pywebview encrypted-ZIP probes confirmed pt-BR/en-US password prompt, session count, retry inspection, and clear state. | None known for the covered ZIP prompt/delete and artifact-confirmation flows. |
| A7 | Confirmed, fixed | P2 | CSV exported for spreadsheets can preserve formula-like values that spreadsheet apps may execute. | `src/gt_dataslicer/export/csv.py`, `src/gt_dataslicer/config.py`, `src/gt_dataslicer/cli.py`, `src/gt_dataslicer/ui/web/app.js` | Spreadsheet formula-injection examples and tests. | Added explicit spreadsheet-safe CSV mode while preserving raw CSV by default; exposed the same option in the UI for CSV outputs. | `tests/unit/test_artifacts.py`, `tests/integration/test_cli.py`, `tests/unit/test_ui_assets.py` | None known. |
| A8 | Confirmed, fixed | P1 | Empty membership lists and NULL membership semantics could produce surprising SQL. | `src/gt_dataslicer/filters/parser.py`, `src/gt_dataslicer/filters/compiler.py`, `src/gt_dataslicer/ui/filters.py` | Parser/compiler/UI filter tests. | Reject empty membership lists and NULL membership values with actionable errors. | `tests/unit/test_parser.py`, `tests/unit/test_compiler.py`, `tests/unit/test_ui_filters.py` | None known. |
| A9 | Confirmed, fixed | P1 | Key-based dedupe survivor was arbitrary without a sort key. | `src/gt_dataslicer/engine/duckdb_engine.py` | Dedupe integration tests. | Require sort keys for key-based dedupe; use them in window order. | `tests/integration/test_cli.py` | None known. |
| A10 | Confirmed, fixed | P1 | Timezone-aware datetime literals could be silently made naive. | `src/gt_dataslicer/filters/parser.py`, `src/gt_dataslicer/filters/compiler.py` | Date/datetime parser/compiler tests. | Reject timezone-aware datetime literals with an actionable message. | `tests/unit/test_parser.py`, `tests/unit/test_compiler.py` | Timezone-preserving execution remains deferred. |
| A11 | Confirmed, fixed for execution runs | P2 | A run can read one physical source multiple times for inspection, execution, reports, rejects, and preview. | `src/gt_dataslicer/engine/duckdb_engine.py`, `src/gt_dataslicer/ui/api.py` | Code path trace, DuckDB current docs for `CREATE TEMP TABLE AS SELECT` and session-scoped temporary tables that can spill with `temp_directory`, and engine tests. | `materialize_source=True` now consistently creates one DuckDB temporary source relation for CSV, Parquet, and staged Excel inputs, so export queries and report row counts use the same prepared relation; reject accounting remains scan-scoped/deduped. | `tests/unit/test_engine.py`, `tests/unit/test_ui_api.py`, `tests/integration/test_cli.py` | Schema inspection still performs an earlier scan, and UI validate/preview are separate bounded operations; a fuller `PreparedRun` boundary could share more preparation state in the future. |
| A12 | Confirmed, fixed | P1 | XLSX input is a ZIP container and needed resource-exhaustion limits. | `src/gt_dataslicer/inputs.py` | XLSX member/size/dimension tests. | Added member count, uncompressed size, compression ratio, worksheet count, and staged-cell limits. | `tests/unit/test_inputs.py` | Limits may need tuning with real customer workbooks. |
| A13 | Confirmed, fixed | P1 | Excel export needed safer workbook/sheet boundaries, derived formula references, and protection against Excel-specific value loss. | `src/gt_dataslicer/export/excel.py`, `src/gt_dataslicer/derived.py`, `README.md` | XlsxWriter behavior for `write()`, `write_string()`, `write_number()`, constant-memory row order, formula cached values, and Excel's 15-digit numeric precision; Excel export tests. | Hardened sheet naming, limits, close behavior, formula/materialization policy, exact-text output for Decimals and integers beyond Excel's exact numeric precision, and explicit failure for strings Excel would truncate. | `tests/unit/test_excel.py`, `tests/unit/test_derived.py`, `tests/unit/test_i18n.py` | Excel formula cache correctness depends on safe supported transformations. |
| A14/B17 | Confirmed, fixed | P1 | String booleans such as `"false"` could be treated as true in config/UI paths; config load could silently drop unsupported options; UI config saves used a direct write; lookup definitions were accepted by the bridge but had no visible load/save workflow. | `src/gt_dataslicer/config.py`, `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/ui/web/styles.css` | Config/UI API tests and UI asset checks. | Explicit bool parsing; UI config-load allowlist; browser-side `configBool`; UI config saves now use the shared atomic text writer; advanced CSV lookup cards load/save through the shared lookup schema and reject incomplete rows instead of dropping them. | `tests/unit/test_config.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py` | Rendered lookup-card focus and file-picker behavior still needs browser verification. |
| A15/C | Confirmed, fixed | P1 | Important resources and jobs needed explicit lifecycle/cancellation behavior. | `src/gt_dataslicer/engine/duckdb_engine.py`, `src/gt_dataslicer/runner.py`, `src/gt_dataslicer/ui/jobs.py`, `src/gt_dataslicer/ui/app.py` | Lifecycle/cancellation tests, Context7 Pywebview docs for `window.events.closing` cancellation, native Pywebview close-guard probe, and native Pywebview cancel-completion probe. | Engine context manager/close, cancellation registration, job state handling, and close-guard notification scheduled outside the close event so the handler can promptly return `False`. | `tests/unit/test_engine.py`, `tests/unit/test_runner.py`, `tests/unit/test_ui_jobs.py`, `tests/integration/test_ui_launch.py`; native Pywebview close-guard probe verified attempted close while running keeps the rendered window open and shows localized guidance; native Pywebview cancel-completion probe verified the rendered run state reaches cancelled and controls reset after cancellation. | None known for the covered lifecycle state transitions. |
| B1/B3 | Confirmed, fixed | P1 | Incomplete visual filter rows and partially configured derived-column cards could be silently omitted or only surfaced late. | `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/web/styles.css`, `src/gt_dataslicer/ui/filters.py` | UI asset, filter conversion, derived-card validation tests, and native Pywebview inline-validation probe. | Inline validation, focus/scroll to first invalid field, Python conversion rejects incomplete rows, and derived-column cards now block validate/preview/run/save for missing source, missing position target, missing transform parameters, invalid counts, or conflicting case transforms. | `tests/unit/test_ui_assets.py`, `tests/unit/test_ui_filters.py`; Pywebview runtime probe confirmed an incomplete visual row is marked invalid with `aria-invalid` and blocks preview. | Exact scroll-to-focused-field positioning remains visually inferred rather than coordinate-measured. |
| B2 | Confirmed, fixed | P2 | Step navigation was only anchors with no workflow state. | `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/web/styles.css` | UI asset tests, rendered static-browser checks at 960x640 and a constrained 768x512 viewport, and native Pywebview workflow probe. | Added per-step state and summaries for file/filter/output/run. | `tests/unit/test_ui_assets.py`; Browser checks at `http://127.0.0.1:8765/index.html`; Pywebview runtime probe verified file/filter/output/run summaries through inspect, preview, export completion, and en-US language switch. | None known for the core single-input workflow. |
| B4 | Confirmed, fixed | P1 | UI inspection/validation described only the first queued input. | `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/app.js` | Multi-input UI API tests. | Inspect/validate all resolved inputs; surface schema mismatches per input. | `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py` | Schema grouping optimization remains future work. |
| B6 | Confirmed, partly fixed | P3 | Read options were shown as one mixed advanced group across CSV, XLSX, ZIP, and queue behavior. | `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/ui/web/app.js` | UI markup/code inspection and UI asset tests. | Grouped advanced read options by CSV, Excel, ZIP, and queue context; irrelevant groups are hidden, disabled, and suppressed from the payload. | `tests/unit/test_ui_assets.py` | ZIP member selection and richer recovery controls remain future UX work. |
| B7 | Confirmed, fixed and rendered-checked | P2 | ZIP password handling needed session-only behavior and less persistent UX. | `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/inputs.py` | ZIP workflow tests, UI asset tests, and native Pywebview encrypted-ZIP probes. | Added prompt-on-needed retry, contextual ZIP controls, in-memory session-only password storage, clear/count controls, and clearing password values on terminal states, config save, and input-queue changes; removed the visible multiline password field. | `tests/unit/test_ui_assets.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_inputs.py`; Pywebview encrypted-ZIP probes verified pt-BR and en-US prompt text, one session password retained, clear button enabled/disabled states, successful retry inspection, and no horizontal overflow. | None known. |
| B8 | Confirmed, fixed | P2 | Visual filter controls ignored inspected column types. | `src/gt_dataslicer/ui/web/app.js` | UI asset tests. | Store inspected column types, restrict operators, adjust value inputs, send semantic value types. | `tests/unit/test_ui_assets.py` | Browser-rendered operator behavior still needs runtime check. |
| B9 | Already protected, strengthened and rendered-checked | P2 | Stale advanced filter text could have overridden visible visual rules after mode switching, and inactive-mode edits needed clearer status. | `src/gt_dataslicer/ui/web/app.js` | Payload code inspection, filter-mode summary inspection, static regression tests, and rendered static-browser check. | Active filter mode is explicit in state and payload uses only visual rules or advanced text for the selected mode; workflow summary now says when visual or advanced edits exist but are inactive. | `tests/unit/test_ui_assets.py`; `node --check src/gt_dataslicer/ui/web/app.js`; Browser check at 960x640 verified `Sem regras; avançado inativo`, English `No rules; advanced inactive`, tab state, scrollability, and `document.documentElement.lang`. | Opposite visual-inactive summary is statically covered; completing a visual rule in the static browser needs an inspected schema or live bridge. |
| B10 | Confirmed, fixed | P2 | Users had no bounded row preview before export, and queued previews could be mistaken for all files. | `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/web/index.html` | Preview API and UI asset tests; native Pywebview rendered preview probe. | Added `preview_rows` API and rendered table preview using same filter/projection path, capped at 100 rows; queued previews now identify that they sample the first file. | `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py`; Pywebview runtime probe verified a filtered one-row preview with derived `CPF_LIMPO` column. | Preview remains first-input-only by design for bounded cost. |
| B11/B12 | Confirmed, fixed | P2 | Output format/destination choices lacked plain-language consequences, multi-output folder behavior, and a clear warning for split Excel outputs. | `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/api.py` | Pywebview file-dialog docs; UI API/assets tests; static rendered output-format check; native Pywebview CSV output and split-output notice probes. | Added format help text, folder picker for multi-input outputs, load/save support for safe Excel split settings, and an output-step notice naming the split XLSX filename pattern. | `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py`; Browser static check verified XLSX card selection and output-step summary; Pywebview runtime probe verified CSV output summary, overwrite retry, output list, and generated file contents; Pywebview notice probe verified pt-BR/en-US split XLSX filename-pattern notice and `outputNeedsFolder()` for multi-input state. | Native OS folder picker interaction itself remains API/fake-window covered rather than manually clicked in a live file dialog. |
| B13 | Confirmed, fixed | P2 | Advanced output options were grouped by implementation surface, and the existing summary/summary-only controls were accidentally removed. | `src/gt_dataslicer/ui/web/index.html`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/api.py` | UI markup/code inspection, comparison with `origin/main`, UI API tests, and UI asset tests. | Grouped advanced output controls into summary, columns, organization, and diagnostics sections while restoring summary-only and filtered-data-plus-summary payload/config behavior. | `tests/unit/test_ui_assets.py`, `tests/unit/test_ui_api.py` | None known for restored static UI/config behavior; rendered summary-control verification remains a future browser check. |
| B14/B15 | Confirmed, partly fixed; rendered keyboard reorder checked | P2 | Derived-column/accessibility controls needed keyboard-operable reordering, clearer summaries, grouped transformations, specific parameter labels, meaningful icon-button names, a simple primary output-name workflow, inline required-field validation, and before/after samples where possible. | `src/gt_dataslicer/ui/web/*`, `src/gt_dataslicer/derived.py` | UI asset tests, derived-column parser tests, native Pywebview derived-card probe, native Pywebview reorder/focus probe, and in-app Browser keyboard probe. | Added up/down controls, summaries, a primary output-name field, transformation optgroups by intent, visible parameter labels, localized aria-labels for punctuation icon buttons, card-local before/after sample preview, inline validation for required derived-column fields, and explicit Enter/Space handling for transform move buttons while preserving config compatibility and Python/DuckDB execution. | `tests/unit/test_ui_assets.py`, `tests/unit/test_derived.py`; Pywebview runtime probe verified fuzzy source-column selection, `CPF_LIMPO` summary, keep-digits before/after sample, move-button accessible names, preview, and exported derived value; Pywebview reorder probe verified localized up/down labels and titles, transform order changes, edge disabled states, focus restoration to the moved transform select, and no horizontal overflow; Browser rendered probe verified Enter moved `trim, keep_digits` to `keep_digits, trim`, Space moved it back, focus returned to the moved transform selector, and there was no horizontal overflow. | Broader high-contrast and extreme zoom accessibility checks remain future work under the rendered-verification residuals. |
| B16 | Confirmed, fixed and rendered-checked | P1 | Running jobs needed cancellation, second-job protection, bounded completed-job storage, and a window-close guard; Cancel could also appear before a bridge `job_id` existed, making an immediate click a no-op during job startup; close attempts while running needed visible guidance. | `src/gt_dataslicer/ui/jobs.py`, `src/gt_dataslicer/ui/web/app.js`, `src/gt_dataslicer/ui/app.py` | Job tests; Pywebview docs for JS bridge worker-thread calls and `events.closing` cancellation by returning `False`; native Pywebview slow-run probe for visible running controls; native Pywebview controls probe for startup/running/canceling button states; native Pywebview close-guard probe; native Pywebview cancel-completion probe. | Added job locking/snapshot copies, bounded completed-job pruning, non-daemon workers, cancel button, DuckDB interrupt path, and close checks; changed the browser UI so Cancel is visible but disabled while a run is starting and is enabled only after `state.activeJobId` is assigned; close guard now schedules a localized rendered notification outside the close event before returning `False`. | `tests/unit/test_ui_jobs.py`, `tests/integration/test_ui_launch.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_ui_assets.py`; `node --check src/gt_dataslicer/ui/web/app.js`; Pywebview slow-run probe verified active bridge job state, disabled Run button, visible enabled Cancel button, running step summary, no horizontal overflow, and temp cleanup; Pywebview controls probe verified startup disabled Cancel, running enabled Cancel, and canceling disabled Cancel; native Pywebview close-guard probe verified attempted close while running keeps the rendered window open and shows the localized close-blocked status/run-step message; native Pywebview cancel-completion probe verified running status (`Gerando o arquivo...`), canceling status (`Cancelando...`), terminal cancelled status (`Execução cancelada.`), hidden Cancel button, re-enabled Run button, cleared active job id, and no horizontal overflow. | None known for the covered run lifecycle states. |
| D1/D2 | Confirmed, partly fixed | P2 | UI and CLI validation paths used private engine methods or duplicated projection/filter preparation. | `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/cli.py`, `src/gt_dataslicer/engine/duckdb_engine.py` | Code inspection and boundary regression tests. | Added public `DuckDBEngine.prepare_filter_query()` and moved UI validation/preview plus CLI `validate-filter` to that shared preparation boundary; source materialization is now part of that boundary instead of a typed-CSV special case. | `tests/unit/test_engine.py`, `tests/unit/test_ui_api.py`, `tests/integration/test_cli.py` | A fuller `PreparedRun` model for artifact planning and reporting remains future architecture work. |
| D3 | Confirmed, fixed for known current paths | P2 | Some localized errors were produced by parsing English exception strings with exact matches and regexes. | `src/gt_dataslicer/i18n.py`, `src/gt_dataslicer/exceptions.py`, `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/filters/*`, `src/gt_dataslicer/config.py`, `src/gt_dataslicer/derived.py`, `src/gt_dataslicer/inputs.py`, `src/gt_dataslicer/export/excel.py`, `src/gt_dataslicer/engine/duckdb_engine.py`, `src/gt_dataslicer/runner.py` | Code inspection, repository-wide raise-site search, structured localization tests, CLI/UI error payload tests. | Added stable `code` and `context` fields to known parser/compiler/config/UI/input/export/engine/runner validation errors, including UI numeric option parsing and bridge guards, visual-filter conversion, config file/preset/list/lookup/rename/sort/type/output parsing, parser date/datetime helpers, compiler coercion/lookup/string-operator checks, derived-column load/parse/projection validation, input/ZIP/Excel read failures, XLSX export limit checks, DuckDB/reject-file wrappers, and artifact-collision preflight guards; CLI/UI rendering now prefers structured localization while preserving legacy fallback. | `tests/unit/test_i18n.py`, `tests/unit/test_compiler.py`, `tests/unit/test_parser.py`, `tests/unit/test_config.py`, `tests/unit/test_derived.py`, `tests/unit/test_inputs.py`, `tests/unit/test_excel.py`, `tests/unit/test_engine.py`, `tests/unit/test_runner.py`, `tests/unit/test_ui_filters.py`, `tests/unit/test_ui_api.py`, `tests/unit/test_ui_jobs.py`, `tests/integration/test_cli.py` | Legacy exact/prefix fallback remains intentionally for compatibility and unexpected string-only errors; new expected `DataSlicerError` raise sites should include stable `code` and `context`. |
| D4 | Partly protected, strengthened | P2 | Broad exception conversion remains in CLI/UI/job boundaries and cleanup paths; some internal expected-error wrappers could hide programming failures. | `src/gt_dataslicer/cli.py`, `src/gt_dataslicer/ui/api.py`, `src/gt_dataslicer/ui/jobs.py`, `src/gt_dataslicer/export/*`, `src/gt_dataslicer/engine/duckdb_engine.py` | Code inspection, UI API bad-input tests, and engine exception-narrowing tests. | Kept broad catches at process/UI/job boundaries where they convert to user-safe errors; cleanup catches preserve original failures; narrowed UI integer parsing into structured `ConfigError` responses; narrowed DuckDB inspection/reject-table probes to `duckdb.Error` so unexpected programming errors are not mislabeled as user input problems. | `tests/unit/test_ui_api.py`, `tests/unit/test_engine.py`, existing CLI/UI/job/export tests. | Boundary and cleanup catches intentionally remain broad; internal parse/preparation code can continue narrowing expected exceptions over time. |
| D5 | Confirmed, partly fixed | P2 | `DuckDBEngine.run_filter()` owned inspection, planning, query assembly, export dispatch, and report updates. | `src/gt_dataslicer/engine/duckdb_engine.py` | Code inspection. | Extracted shared filter/query preparation into `prepare_filter_query()` while leaving export/report ownership in `run_filter()`. | `tests/unit/test_engine.py`, `tests/unit/test_ui_api.py` | Export dispatch/report updates remain in `run_filter()` until a broader prepared-run/report boundary is justified. |
| D6 | Already protected, strengthened | P2 | Reports and overwrite preflight must reflect artifacts that can actually be committed, including split XLSX sibling files. | `src/gt_dataslicer/report.py`, `src/gt_dataslicer/runner.py`, `src/gt_dataslicer/export/*` | Report/output path tests and artifact collision tests. | Reports are written after runs finish; queue reports aggregate completed child reports; artifact collision checks prevent report/output/reject path overlap; preflight artifact planning now includes existing split XLSX siblings beyond the first file. | `tests/unit/test_artifacts.py`, `tests/unit/test_runner.py`, `tests/unit/test_ui_api.py`, `tests/integration/test_cli.py` | Very large split-output counts are still impractical to exhaustively generate in normal tests, but the naming-pattern preflight is covered. |
| D7 | Confirmed, partly fixed | P3 | Dependencies were minimum-version ranges and there was no constraints file or CI config. | `pyproject.toml`, `requirements-release.txt`, `README.md` | Packaging metadata inspection and release-baseline tests. | Added a lightweight direct-dependency release constraints file and README install command for reproducing the tested dependency baseline. | `tests/unit/test_freezing.py` | This is not a full transitive lockfile and CI remains future release work. |
| Rendered verification | Substantially completed, residuals scoped | P1 | Required rendered local UI checks need evidence in addition to static tests. | Browser skill / local static harness; native Pywebview runtime probe | Context7 Pywebview docs for `http_server=True`, `webview.start(func, window)`, `window.evaluate_js`, `window.run_js`, `window.destroy`, `window.events.closing` cancellation by returning `False`, and `zoomable`; Browser static checks; native Pywebview debug launch and runtime probes. A browser-side mock bridge attempt was stopped because the Browser security policy blocked bookmarklet-style execution and disallowed workarounds; after the startup-cancel UI edit, one Browser static-server smoke attempt timed out in navigation even though `Invoke-WebRequest` returned 200, so it was not counted as passing evidence; a later Browser session successfully loaded the static UI for keyboard verification. Browser CUA `Ctrl+plus`/`Ctrl+equals` and CDP page-scale attempts did not change observable zoom metrics, so they were not counted as true zoom evidence. A later alternate Playwright/CDP zoom attempt timed out during navigation and was also not counted as evidence. | Performed targeted rendered checks for B9 filter-mode summaries at 960x640, broader static checks for language/output/layout, and a native Pywebview workflow probe with the real JS bridge: inspect real CSV, inline validation, keyboard column search, visual filter payload, derived-column sample, preview, overwrite confirmation retry, completed export, generated-file inspection, pt-BR/en-US switch, scrolling, and no horizontal overflow in the 960x640 window. Slow-run and controls Pywebview probes verified visible running controls plus startup/running/canceling Cancel button states. Notice and encrypted-ZIP probes verified split-output notices, multi-input folder requirement state, ZIP-delete confirmation text/flag, and pt-BR/en-US ZIP-password prompt/count/retry behavior in rendered Pywebview. Derived reorder probes verified rendered up/down controls, order changes, disabled states, focus restoration, Enter/Space keyboard activation, and no horizontal overflow. Native close-guard probe verified attempted close while running keeps the window open and shows localized status/run-step guidance; an earlier synchronous-notification attempt hung and was replaced with asynchronous notification after the close veto. Native cancel-completion probe verified running, canceling, and terminal cancelled states, hidden Cancel button, re-enabled Run button, cleared active job id, and no horizontal overflow. A zoom-layout probe confirmed normal 960x640 and 480px CSS viewport have no horizontal overflow; 240px initially had small overflow from the two-column step nav, so an extreme-narrow fallback now collapses steps to one column and the cache-busted 240px Browser check passes with no horizontal overflow. `body.style.zoom=125%` creates transform-scale overflow, but Context7 docs and the available Browser/Pywebview automation did not expose a reliable true page-zoom test API. Temporary input/output files, probe folders, local static server, and Pywebview user-data temp folders were removed. | Browser checks plus Pywebview runtime probes recorded in this ledger; automated API/static tests cover remaining bridge semantics. | Still not live-rendered: true browser/Pywebview zoom-level behavior through an actual page-zoom control, because available automation did not change zoom metrics or could not reliably navigate for the alternate zoom attempt. Responsive pressure checks at 480px and 240px now pass. |
